# Admin (Next.js)
Create app:
```bash
npm create next-app@latest . -- --ts --app --eslint --src-dir --import-alias "@/*"
npm install
npm run dev
```

Suggested structure:
```
app/admin/(routes)/
components/
lib/api/
lib/auth/
```
